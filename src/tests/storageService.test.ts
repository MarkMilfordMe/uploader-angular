import {StorageService} from '../StorageService';

describe('StorageService',  () => {

	it("stores file changes", () => {
		expect(2).toEqual(1);
		new StorageService();
		//expect(new StorageService().getExistingItems()).
		//to/Equal([]);
	});
	
});